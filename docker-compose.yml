version: '3.8'

services:
  agent:
    build: .
    container_name: gespro_agent
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    command: uvicorn src.voice.server:app --host 0.0.0.0 --port 8000 --reload
